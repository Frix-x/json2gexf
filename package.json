{
  "name": "json2gexf",
  "version": "1.0.0",
  "description": "JSON parser to CSV optimised for JSON obtained with \"Lost Circles\" chrome extension",
  "main": "convert.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node convert.js"
  },
  "dependencies": {
  }
}
